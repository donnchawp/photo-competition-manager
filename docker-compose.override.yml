version: '3.7'

services:
  # Add Mailpit for email testing
  mailpit:
    image: axllent/mailpit:latest
    container_name: club-competitions-mailpit
    ports:
      - "8025:8025"  # Web UI
      - "1025:1025"  # SMTP server
    networks:
      - wpenv
    environment:
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1

  # Configure WordPress containers to use Mailpit
  wordpress:
    depends_on:
      - mailpit
    environment:
      # WordPress SMTP configuration
      WORDPRESS_CONFIG_EXTRA: |
        /* Mailpit SMTP Configuration */
        define('SMTP_HOST', 'mailpit');
        define('SMTP_PORT', 1025);
        define('SMTP_USER', '');
        define('SMTP_PASS', '');
        define('SMTP_FROM', 'wordpress@club-competitions.local');
        define('SMTP_NAME', 'Club Competitions');

  tests-wordpress:
    depends_on:
      - mailpit
    environment:
      # WordPress SMTP configuration for tests
      WORDPRESS_CONFIG_EXTRA: |
        /* Mailpit SMTP Configuration */
        define('SMTP_HOST', 'mailpit');
        define('SMTP_PORT', 1025);
        define('SMTP_USER', '');
        define('SMTP_PASS', '');
        define('SMTP_FROM', 'wordpress@club-competitions.local');
        define('SMTP_NAME', 'Club Competitions');

networks:
  wpenv:
    external: true
